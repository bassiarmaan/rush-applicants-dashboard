# =============================================================================
# RUSH APPLICANTS DASHBOARD - ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to .env.local and update the values with your actual credentials.
# 
# IMPORTANT: Never commit .env.local to version control!
# =============================================================================

# =============================================================================
# AIRTABLE CONFIGURATION
# =============================================================================
# Required for fetching and managing applicant data from Airtable
# Get these values from your Airtable account settings

# Your Airtable API key (Personal Access Token)
# Generate at: https://airtable.com/create/tokens
AIRTABLE_API_KEY=your_airtable_api_key_here

# Your Airtable Base ID
# Found in your Airtable base URL: https://airtable.com/[BASE_ID]/...
AIRTABLE_BASE_ID=your_airtable_base_id_here

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
# Required for securing access to the dashboard

# Dashboard login password (change this to a secure password)
# Used for admin access to the applicant dashboard
DASHBOARD_PASSWORD=your_secure_password_here

# JWT Secret for token-based authentication
# Generate a strong, random secret key for production
# You can use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# =============================================================================
# DEPLOYMENT & RUNTIME
# =============================================================================
# These are automatically set by deployment platforms

# Node.js environment (development/production)
# NODE_ENV=development

# Vercel deployment environment (preview/production)
# VERCEL_ENV=development

# Vercel deployment URL (automatically set by Vercel)
# VERCEL_URL=your-app.vercel.app

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================
# Uncomment and configure these if needed

# Custom port for development server (default: 3000)
# PORT=3000

# Custom host for development server (default: localhost)
# HOSTNAME=localhost

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Change DASHBOARD_PASSWORD to a strong, unique password
# 2. Generate a secure JWT_SECRET using: openssl rand -base64 32
# 3. Keep your Airtable API key secure and rotate it regularly
# 4. Never share these credentials or commit them to version control
# 5. Use different values for development, staging, and production
# =============================================================================
